<UserControl x:Class="AwbStudio.ProjectConfiguration.PropertyEditors.ScsServoEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AwbStudio.ProjectConfiguration.PropertyEditors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel>
        <Label>SCS Servo</Label>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Label Grid.Column="0" HorizontalAlignment="Right">ID:</Label>
            <TextBox Grid.Column="1" Text="{Binding Id}"/>
            
            <Label Grid.Column="0" Grid.Row="1" HorizontalAlignment="Right">Title</Label>
            <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding Title}"/>
        </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
